 是的，代码中有 Token 使用量追踪和预算管理，但主要在上下文窗口管理层面，而非成本控制层面。让我从代码中梳理具体实现。

  Searched for 2 patterns, read 4 files (ctrl+o to expand)

● 有，但两个层面各自独立，尚未打通。以下是详细分析：

  ---
  1. Token 使用量追踪 — Usage 数据类

  位置： runtime/types.py:138-143

  @dataclass(frozen=True)
  class Usage:
      input_tokens: int = 0
      output_tokens: int = 0
      cache_write_tokens: int | None = None
      cache_read_tokens: int | None = None

  InferenceResult.usage 承载每次推理的用量数据，Provider 从 API 响应中填充：

  ┌───────────────────┬─────────────────────────────────────────────────────────────────┐
  │     Provider      │                            数据来源                             │
  ├───────────────────┼─────────────────────────────────────────────────────────────────┤
  │ AnthropicProvider │ response.usage.input_tokens / response.usage.output_tokens      │
  ├───────────────────┼─────────────────────────────────────────────────────────────────┤
  │ OpenAIProvider    │ response.usage.prompt_tokens / response.usage.completion_tokens │
  └───────────────────┴─────────────────────────────────────────────────────────────────┘

  Pipeline 在 AFTER_INFERENCE 事件中发出 usage=result.usage。

  缺失环节： Session 没有累积每次调用的 Usage，无法查询"本次会话用了多少 token"。

  ---
  2. Token 预算管理 — ContextManager._check_budget()

  位置： context_manager/manager.py:63-72

  class ManagerConfig:
      total_budget: int = 64000          # L1 上下文窗口预算
      warn_ratio: float = 0.65           # 65% → 异步预压缩
      compress_ratio: float = 0.80       # 80% → 开始压缩
      critical_ratio: float = 0.92       # 92% → 强制压缩
      hard_limit_ratio: float = 0.98     # 98% → 紧急压缩

  预算检查触发压缩层级：

  add_message() → _check_budget()
    ├─ ≥65% (WARN)     → 异步提交压缩计划
    ├─ ≥80% (COMPRESS) → 同步压缩，目标回退到 65%
    ├─ ≥92% (CRITICAL) → 强制压缩，目标回退到 80%
    └─ ≥98% (HARD)     → 紧急压缩

  注意： 这里的 "budget" 使用 Message.token_estimate（len(content) // 4）估算，不是 API 返回的真实 token 计数，也不用于限制总消耗——仅用于触发上下文压缩。

  ---
  3. 缺失的功能总结

  ┌──────────────────────┬───────┬─────────────────────────────────────────┐
  │         功能         │ 状态  │                  说明                   │
  ├──────────────────────┼───────┼─────────────────────────────────────────┤
  │ 单次推理用量追踪     │ ✅ 有 │ InferenceResult.usage + Provider 填充   │
  ├──────────────────────┼───────┼─────────────────────────────────────────┤
  │ 会话级总量统计       │ ❌ 无 │ Session 未累积 Usage                    │
  ├──────────────────────┼───────┼─────────────────────────────────────────┤
  │ API 费用计算         │ ❌ 无 │ 无模型单价映射                          │
  ├──────────────────────┼───────┼─────────────────────────────────────────┤
  │ 用户可查询的用量报告 │ ❌ 无 │ 无 CLI/API 接口输出用量                 │
  ├──────────────────────┼───────┼─────────────────────────────────────────┤
  │ 上下文窗口预算控制   │ ✅ 有 │ ContextManager._check_budget() 触发压缩 │
  ├──────────────────────┼───────┼─────────────────────────────────────────┤
  │ 硬性 token 消耗上限  │ ❌ 无 │ 预算仅用于压缩决策，不限制执行          │
  ├──────────────────────┼───────┼─────────────────────────────────────────┤
  │ 真实 token 计数      │ ❌ 无 │ 使用 len//4 估算，非真实 tokenizer      │
  └──────────────────────┴───────┴─────────────────────────────────────────┘

  需要我实现会话级 token 用量累积和查询功能吗？


  设计规划：文件写入工具(WriteTool)和文件编辑工具(EditTool)增强

     Context

     当前 WriteTool 仅支持全文件创建/覆盖，EditTool 仅支持简单字符串替换（首次匹配）。需要扩展这两个工具的能力，使 LLM 能更灵活地操作文件，同时保持向后兼容并完善工具注册。

     ---
     修改文件清单

     ┌─────────────────────────────┬─────────────────────────────────────────┐
     │            文件             │                  变更                   │
     ├─────────────────────────────┼─────────────────────────────────────────┤
     │ tool_registry/file_tools.py │ WriteTool + EditTool 核心逻辑增强       │
     ├─────────────────────────────┼─────────────────────────────────────────┤
     │ harnesses/research.py       │ 注册 EditTool + 更新系统提示            │
     ├─────────────────────────────┼─────────────────────────────────────────┤
     │ harnesses/base.py           │ 在 install_file_tools() 中注册 EditTool │
     ├─────────────────────────────┼─────────────────────────────────────────┤
     │ tests/test_tool_registry.py │ 新增 WriteTool/EditTool 测试            │
     ├─────────────────────────────┼─────────────────────────────────────────┤
     │ tests/test_harnesses.py     │ 更新预期工具集合                        │
     └─────────────────────────────┴─────────────────────────────────────────┘

     ---
     详细变更

     1. WriteTool 增强 (file_tools.py)

     新增参数：
     - mode: str = "write" — "write"（覆盖）/ "append"（追加）
     - insert_at: int | None = None — 在指定行号插入（1-based）
     - create_parents: bool = True — 是否自动创建父目录
     - content_validator: Callable | None = None — 内容验证钩子（构造参数）

     execute() 流程：
     1. 大小检查（已有）→ 编码验证（新增）→ 内容验证钩子（新增）
     2. 模式校验：append + insert_at 冲突 → 报错
     3. Path 解析 + 父目录检查
     4. Append 模式：读取已有内容 + 追加；Insert 模式：在指定行插入
     5. 写入文件 → 返回结果

     向后兼容： 所有新参数有默认值，现有调用无需修改。

     2. EditTool 增强 (file_tools.py)

     新增参数：
     - start_line / end_line: int | None = None — 行范围限制（1-based）
     - use_regex: bool = False — 启用正则匹配
     - occurrence: int = 0 — 0=首次, -1=全部, N=第N次

     execute() 流程：
     1. 读取文件（已有）
     2. 行范围切片：只对指定行区域进行替换
     3. 正则/字面量替换，按 occurrence 控制替换次数
     4. 计算 unified diff（difflib.unified_diff）
     5. 写回文件 → 返回结果（含 diff）

     向后兼容： 所有新参数有默认值。

     3. 工具注册

     - research.py: 注册 EditTool(allowed_roots=allowed)，更新系统提示词增加 edit
     - base.py: install_file_tools() 增加 EditTool 注册

     4. 测试计划

     WriteTool 新测试：
     - append 模式（已有文件 / 新文件）
     - insert_at 行插入（中间 / 首行 / 超出末尾 / 新文件）
     - create_parents=False（父目录存在 / 缺失）
     - content_validator 钩子（拒绝 / 接受）

     EditTool 新测试：
     - use_regex=True（正常 / 全部替换 / 无效正则）
     - occurrence 控制（第N次 / 全部 / 超出）
     - 行范围限制（范围内 / 范围外 / 无效范围）
     - diff 输出验证

     5. 验证方法

     # 工具逻辑测试
     python -m pytest tests/test_tool_registry.py -v

     # Harness 集成测试
     python -m pytest tests/test_harnesses.py -v

     # 完整测试
     python -m pytest tests/ -v

● 计划已通过，开始实施。

✢ Topsy-turvying… (18m 48s · ↓ 8.1k tokens)
  ⎿  □ Enhance WriteTool with append/insert/create_parents/validator
     □ Enhance EditTool with line-range/regex/occurrence/diff
     □ Register EditTool in base.py and research.py
     □ Add tests for WriteTool and EditTool enhancements

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
> 
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  ⏵⏵ bypass permissions on (shift+tab to cycle) · esc to interrupt · ctrl+t to hide tasks     ✗ Auto-update failed · Try claude doctor or npm i -g @anthropic-ai/claude-code

     - research.py: 注册 EditTool(allowed_roots=allowed)，更新系统提示词增加 edit
     - base.py: install_file_tools() 增加 EditTool 注册

     4. 测试计划

     WriteTool 新测试：
     - append 模式（已有文件 / 新文件）
     - insert_at 行插入（中间 / 首行 / 超出末尾 / 新文件）
     - create_parents=False（父目录存在 / 缺失）
     - content_validator 钩子（拒绝 / 接受）

     EditTool 新测试：
     - use_regex=True（正常 / 全部替换 / 无效正则）
     - occurrence 控制（第N次 / 全部 / 超出）
     - 行范围限制（范围内 / 范围外 / 无效范围）
     - diff 输出验证

     5. 验证方法

     # 工具逻辑测试
     python -m pytest tests/test_tool_registry.py -v

     # Harness 集成测试
     python -m pytest tests/test_harnesses.py -v

     # 完整测试
     python -m pytest tests/ -v

● 计划已通过，开始实施。